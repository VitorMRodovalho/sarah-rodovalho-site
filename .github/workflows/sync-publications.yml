name: Orenu publication sync (Phase 1a)

# Per ADR-024-orenu-as-source-of-truth-personal-branding-sites.md.
#
# Runs scripts/sync_publications.py against the Orenu Postgres DB (via the
# ORENU_DATABASE_URL secret) and opens a PR if the run produces any file
# changes. If the run is a no-op (script output identical to prior state),
# no PR is opened — zero-churn idempotency.
#
# Schedule: daily at 06:00 UTC (off-peak; far from typical CI batch windows
# and far from the user's working hours so any "Daily sync" PR notification
# lands fresh in the morning, not mid-day).
#
# Manual trigger: workflow_dispatch enabled so the user can force a sync
# run anytime (e.g., after flipping public_visibility on a new row in
# Orenu and wanting the PR immediately rather than waiting for the daily).
#
# Required GitHub repository configuration (one-time setup):
#   1. Settings → Secrets and variables → Actions → New repository secret
#      Name: ORENU_DATABASE_URL
#      Value: read-only Postgres connection string for the Orenu DB
#             (suggest a dedicated `sync_publications_ro` role scoped to
#             SELECT on public.dim_publication; current setup uses the
#             standard credential — rotate to read-only role in Phase 1b)
#   2. Settings → Actions → General → Workflow permissions:
#      ✓ "Read and write permissions"
#      ✓ "Allow GitHub Actions to create and approve pull requests"
#      (required for the peter-evans/create-pull-request action to open PRs)

on:
  schedule:
    - cron: '0 6 * * *'  # daily 06:00 UTC
  workflow_dispatch:
  # Also run on PRs that touch the script itself so test changes / regex
  # tweaks are CI-validated against the same dry-run logic.
  pull_request:
    paths:
      - 'scripts/sync_publications.py'
      - 'scripts/requirements*.txt'
      - 'tests/test_sync_publications.py'
      - '.github/workflows/sync-publications.yml'

permissions:
  contents: write       # for committing the diff + pushing the branch
  pull-requests: write  # for opening the auto-PR

jobs:
  test:
    # Unit-test the script logic on every trigger (cheap; ~5 seconds).
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip
          cache-dependency-path: scripts/requirements-dev.txt
      - run: pip install -r scripts/requirements-dev.txt
      - run: python -m pytest tests/test_sync_publications.py -v

  sync:
    # Only run the live sync on scheduled / manual triggers — not on PR
    # validation runs (PRs don't have access to the secret anyway).
    if: github.event_name == 'schedule' || github.event_name == 'workflow_dispatch'
    needs: test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip
          cache-dependency-path: scripts/requirements.txt

      - run: pip install -r scripts/requirements.txt

      - name: Run sync (apply mode)
        id: sync
        env:
          ORENU_DATABASE_URL: ${{ secrets.ORENU_DATABASE_URL }}
        run: |
          # Capture stdout + exit code; tee to file so we can show it in
          # the PR body. Non-zero exit codes (drift / stubs / orphans)
          # are NOT fatal — they should surface in the PR for review.
          set +e
          python scripts/sync_publications.py --apply 2>&1 | tee sync-output.txt
          exit_code=${PIPESTATUS[0]}
          set -e
          echo "exit_code=$exit_code" >> "$GITHUB_OUTPUT"

      - name: Detect file changes
        id: diff
        run: |
          if [[ -n "$(git status --porcelain)" ]]; then
            echo "has_changes=true" >> "$GITHUB_OUTPUT"
            git status --porcelain
          else
            echo "has_changes=false" >> "$GITHUB_OUTPUT"
            echo "(no changes — script run is idempotent)"
          fi

      - name: Create or update Pull Request
        if: steps.diff.outputs.has_changes == 'true'
        uses: peter-evans/create-pull-request@v6
        with:
          branch: orenu-sync/publications
          delete-branch: true
          commit-message: |
            chore(orenu-sync): publication frontmatter — daily run

            Generated by .github/workflows/sync-publications.yml.
            Source of truth: Orenu dim_publication WHERE public_visibility = true.

            Assisted-By: Claude (Anthropic) <noreply@anthropic.com>
          title: 'chore(orenu-sync): publication frontmatter — daily run'
          body: |
            ## Phase 1a Orenu → site publication sync

            Automated daily run of `scripts/sync_publications.py --apply`.

            Source of truth: Orenu `dim_publication WHERE public_visibility = true`.
            Per [ADR-024](https://github.com/VitorMRodovalho/rodovalho-finance/blob/main/decisions/ADR-024-orenu-as-source-of-truth-personal-branding-sites.md).

            **Script output:**

            ```
            ${{ steps.sync.outputs.exit_code != '0' && '⚠️ Warnings present (drift / stubs / orphans). Review carefully.' || '✅ Clean.' }}
            ```

            <details>
            <summary>Full sync log</summary>

            ```
            $(cat sync-output.txt)
            ```
            </details>

            ## Review checklist

            - [ ] Drift warnings (if any) — confirm the canonical version
            - [ ] Stubs needed — create MDX files manually for new Orenu public rows
            - [ ] Orphan MDX — flip Orenu `public_visibility = true` OR move MDX to `_archive/`
            - [ ] Citation updates — sanity-check counts vs Scholar before merge

            Auto-generated; safe to close without merging if changes look wrong.
          labels: |
            orenu-sync
            automated

      - name: Summary when no changes
        if: steps.diff.outputs.has_changes != 'true'
        run: echo "::notice ::No file changes from sync run — fully idempotent."
